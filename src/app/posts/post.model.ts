export interface Post { //Look like class, define how object looks like but it can't be instantiated
  id: string; //Auto create by mongoose
  title: string;
  content: string; //Defind how properties look like
}
